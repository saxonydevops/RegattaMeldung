@model IEnumerable<RegattaMeldung.Models.Raceclass>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Length)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Length)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RaceclassId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RaceclassId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RaceclassId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
