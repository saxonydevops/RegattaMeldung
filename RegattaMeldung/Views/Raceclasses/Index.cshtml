@model IEnumerable<RegattaMeldung.Models.Raceclass>

@{
    ViewData["Title"] = "Rennklassen";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

    <h2>Rennklassen</h2>

<p>
    <a class="btn btn-success" asp-action="Create">Neue Rennklasse</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Length)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Length)
            </td>
            <td>
                <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.RaceclassId">Bearbeiten</a> |
                <a class="btn btn-secondary" asp-action="Details" asp-route-id="@item.RaceclassId">Details</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.RaceclassId">L&ouml;schen</a>
            </td>
        </tr>
}
    </tbody>
</table>
