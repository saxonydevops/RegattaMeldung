@model IEnumerable<RegattaMeldung.Models.Oldclass>

@{
    ViewData["Title"] = "Altersklassen";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Altersklassen</h2>

<p>
    <a class="btn btn-success" asp-action="Create">Neue Altersklasse</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.FromAge)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ToAge)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FromAge)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ToAge)
            </td>
            <td>
                <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.OldclassId">Bearbeiten</a> |
                <a class="btn btn-secondary" asp-action="Details" asp-route-id="@item.OldclassId">Details</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.OldclassId">L&ouml;schen</a>
            </td>
        </tr>
}
    </tbody>
</table>
