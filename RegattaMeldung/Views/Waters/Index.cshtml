@model IEnumerable<RegattaMeldung.Models.Water>

@{
    ViewData["Title"] = "Gewässer";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Gew&auml;sser</h2>

<p>
    <a class="btn btn-success" asp-action="Create">Neues Gew&auml;sser</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.WaterId">Bearbeiten</a> |
                <a class="btn btn-secondary" asp-action="Details" asp-route-id="@item.WaterId">Details</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.WaterId">L&ouml;schen</a>
            </td>
        </tr>
}
    </tbody>
</table>
